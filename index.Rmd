--- 
title: "Bulletdown: A bookdown-based Bullet Journal"
date: "Last update: `r Sys.Date()`"
site: bookdown::bookdown_site
output: bookdown::gitbook
documentclass: book
github-repo: tgerke/bulletdown
description: "This is a template for a Bullet Journal that is built with the bookdown package. The output format for this example is bookdown::gitbook."
---

# Index

<!-- load packages, initialize/load the calendar --> 
```{r  setup, include=FALSE}
require(tidyverse)
require(tibbletime)

# these lines only run on the first implementation to store calendar.RData
# dates <- as_tbl_time(data.frame(date=Sys.Date()), date)
# dates <- dates %>% mutate(month=substr(months(date), 1, 3),
#                           day=substr(weekdays(date), 1, 1))
# save(dates, file="calendar.RData")

load("calendar.RData")
```

[Future log](#futurelog)

[Monthly log](#monthlylog)

[Daily log](#dailylog)

[Collections](#collections)
